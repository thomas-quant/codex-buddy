{
  "type": "object",
  "additionalProperties": false,
  "required": ["emit", "text", "tone"],
  "properties": {
    "emit": { "type": "boolean" },
    "text": {
      "type": ["string", "null"],
      "maxLength": 120
    },
    "tone": {
      "type": ["string", "null"],
      "enum": ["pleased", "amused", "concerned", "sleepy", "impressed", "neutral", null]
    }
  }
}
